// =============

let name = 'bob';
console.log(`hi ${name}`);

let line = `
                ahddddddddddddbyikajdnoik
                nknofsilnlsnfoinsdi
                yikdnlifpoodf['p'f;[]]
                kismkdmlkmpeqo[pdep[[d]
            `;

console.log(line);

let getResponse = function () {
    return 1;
}

let interpol = console.log((getResponse() == 1) ? 'it is one' : 'it is more than one');